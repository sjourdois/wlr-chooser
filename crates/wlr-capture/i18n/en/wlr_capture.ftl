# English (fallback) UI strings for the wlr-utils tools (wlr-chooser, wlr-pip).
# The command line (clap) is English-only and not localised.

tab-all = All
tab-windows = Windows
tab-outputs = Screens

filter-hint = Filter…
screen-label = Screen { $name }
loading = …
show-system = System windows

error = wlr-chooser: { $error }
pip-gone = Source window closed
